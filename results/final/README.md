# Kết quả full 49 — đang chờ chạy GPU

Thư mục này được để trống có chủ đích trong bản đóng gói hiện tại.

Chạy `python scripts/run_full_pipeline.py --skip-train` theo `docs/RUN_FULL_49_AND_DEPLOY.md`. Pipeline sẽ dùng checkpoint đã huấn luyện, tuning chỉ trên validation và ghi kết quả Test 49 điều kiện vào đây.

Không sử dụng số liệu trong `results/quick_smoke_not_for_report/` cho báo cáo hoặc kết luận khoa học.
